namespace TestRail.Utils
{
    /// <summary>
    /// Command type's available
    /// </summary>
    public enum CommandType
    {
        get,
        add,
        update,
        delete,
        close
    }
}